syntax on
" Renders each tab byte (\x09) as the number of character blocks from the left
set tabstop=2
" How many spaces the vi cmd >> moves
set shiftwidth=2
" Replaces tab byte with space byte
set expandtab
set ai
set number
set hlsearch
set ruler
highlight Comment ctermfg=green
